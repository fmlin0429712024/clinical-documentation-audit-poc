[project]
name = "claude-agent-sdk-audit"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk>=0.2.129",
    "mcp>=1.29.0",
]
